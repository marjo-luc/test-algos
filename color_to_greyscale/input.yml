# Sample CWL job file: cwltool <cwl> input.yml
#
# Primitives are plain scalars. File and Directory need a `class` plus either a
# `path` (local, relative to this file) or a `location` (URI).
input_image:
  class: File
  path: sample_rgb.png
output_file: greyscale.tif
